export default function Home() {
  // ui menggunakan table
  // filter selesai/tidak selesai
  // didalam table ada info singkat (todo, status), action (update, delete, detail)
  return <>list data</>;
}

